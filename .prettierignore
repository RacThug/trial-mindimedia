.next/
node_modules/
out/
next-env.d.ts
package-lock.json

# Prose is hand-wrapped and the PRD's tables are hand-aligned. Prettier reflows
# both, producing large diffs that carry no information. Markdown is reviewed by
# reading it, not by a formatter.
*.md
docs/
